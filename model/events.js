Events.allow({
  insert : function () {
    return true;
  },
  update : function () {
    return true;
  },
  remove : function () {
    return true;
  }
});

// Use Meteor.methods for db operations
Meteor.methods({
});

